# RESTFul-API

A RESTFul API built over Python Flask framework in order to POST request to crawl data alongwith GET request to retrieve the data. It implements a complete Extract, Transform and Load (ETL) Pipeline that extracts data from websites and loads them to ElasticSearch after transformation.

# Technologies Used
- Python Flask
- JSON
- Requests
- Elastic Search
- Beautiful Soup
